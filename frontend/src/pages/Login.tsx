import {FormEvent,useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {post} from '../services/api';
export function Login(){const nav=useNavigate(); const [email,setEmail]=useState('admin@fitmanager.com'); const [senha,setSenha]=useState('123456'); const [erro,setErro]=useState(''); async function submit(e:FormEvent){e.preventDefault();try{const r=await post<{token:string}>('/auth/login',{email,senha});localStorage.setItem('fitmanager_token',r.token);nav('/')}catch(err){setErro(err instanceof Error?err.message:'Erro')}} return <div className="login"><form className="card" onSubmit={submit}><h1>FitManager</h1><p>Gestão inteligente para academias.</p><input placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" placeholder="Senha" value={senha} onChange={e=>setSenha(e.target.value)}/>{erro&&<p className="error">{erro}</p>}<button className="btn">Entrar</button></form></div>}
