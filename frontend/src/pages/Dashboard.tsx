import {useEffect,useState} from 'react'; import {get} from '../services/api'; import {Card} from '../components/ui/Card';
export function Dashboard(){const [stats,setStats]=useState({alunos:0,planos:0,treinos:0,freq:0}); useEffect(()=>{Promise.all([get<unknown[]>('/api/alunos'),get<unknown[]>('/api/planos'),get<unknown[]>('/api/treinos'),get<unknown[]>('/api/frequencias')]).then(([a,p,t,f])=>setStats({alunos:a.length,planos:p.length,treinos:t.length,freq:f.length})).catch(()=>{})},[]); return <><h1>Dashboard</h1><p>Visão geral da academia.</p><div className="grid">{[['Alunos',stats.alunos],['Planos',stats.planos],['Treinos',stats.treinos],['Frequências',stats.freq]].map(([l,v])=><Card key={String(l)}><small>{l}</small><strong className="metric">{v}</strong></Card>)}</div><Card><h2>Objetivo do FitManager</h2><p>Centralizar a gestão da academia, reduzindo processos manuais e oferecendo uma visão administrativa dos dados.</p></Card></>}
