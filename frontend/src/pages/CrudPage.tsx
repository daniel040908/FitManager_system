import {useEffect,useState} from 'react'; import {get,post} from '../services/api'; import {Button} from '../components/ui/Button'; import {Card} from '../components/ui/Card';
export function CrudPage({title,path,fields}:{title:string;path:string;fields:{key:string;label:string;type?:string}[]}){const [items,setItems]=useState<Record<string,unknown>[]>([]); const [form,setForm]=useState<Record<string,string>>({}); const load=()=>get<Record<string,unknown>[]>(path).then(setItems).catch(()=>setItems([])); useEffect(()=>{load()},[]); async function submit(e:React.FormEvent){e.preventDefault();try{await post(path,Object.fromEntries(fields.map(f=>[f.key,form[f.key]])));setForm({});load()}catch(err){alert(err instanceof Error?err.message:'Erro')}} return <><h1>{title}</h1><div className="two"><Card><h2>Novo cadastro</h2><form onSubmit={submit}>{fields.map(f=><input key={f.key} type={f.type||'text'} placeholder={f.label} value={form[f.key]||''} onChange={e=>setForm({...form,[f.key]:e.target.value})} required={f.key!=='descricao'}/>}<Button type="submit">Cadastrar</Button></form></Card><Card><h2>Registros</h2>{items.length===0?<p>Nenhum registro encontrado.</p>:<div className="table">{items.map((item,i)=><div className="row" key={String(item.id||i)}><strong>{String(item.nome || (item.usuario as {nome?: string} | undefined)?.nome || item.status || 'Registro')}</strong><span>{Object.entries(item).filter(([k])=>!['id','usuario','plano'].includes(k)).slice(0,3).map(([k,v])=><span key={k}> {k}: {String(v)} </span>)}</span></div>)}</div>}</Card></div></>}
